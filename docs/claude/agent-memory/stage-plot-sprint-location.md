---
name: stage-plot-sprint-location
description: "Where the Stage Plot Builder sprint runs — clone, branch, migration numbering"
metadata:
  node_type: memory
  type: project
  originSessionId: 1856bd56-4fe4-4f93-b9a0-938e7c5853eb
---

The Stage Plot Builder sprint runs in the **`/Users/lowpass/Documents/lowpass-app`** clone, NOT the Google Drive clone that the harness opens as cwd. The two are separate checkouts of `github.com/Lowpass-co/lowpass-app.git` and have diverged: the Drive clone is on `design/ux-audit-2026` and lacks the sprint docs; the Documents clone is on `main` and holds `docs/handover/PARKED_STAGE_PLOT_BUILDER.md` + all CC sprint docs + migrations through 106.

- Work on branch **`feat/stage-plot-builder`** (off Documents `main`).
- The shell cwd resets to the Drive clone after every Bash call — always use absolute `/Users/lowpass/Documents/lowpass-app/...` paths and `git -C`. Read/Write/Edit DO reach the Documents path despite it being outside cwd.
- Migration numbering: `main` tops out at 106, but `design/ux-audit-2026` carries 107 (`advance_intake_links`) + 108 (`advance_intake_notification_dedup`). §SP0 used **109**; number future migrations after the highest across ALL branches, not just main.
- §SP0 (migration 109 + `_apply_109_supabase.sql` + `src/lib/types/stage-plot.ts`) committed as `4a893e2`. Adam applies migration SQL by hand in the Supabase SQL Editor (paste the `_apply_NNN` file — `--` comments only, never `/* */`).

**Visual verification of UI:** the preview browser is UNauthenticated, and `src/lib/supabase-middleware.ts` redirects non-public routes to `/login`. To screenshot stage-plot UI: (1) preview_start the `stage-plot-dev` launch config (added to the Drive clone's `.claude/launch.json`; runs `npm --prefix /Users/lowpass/Documents/lowpass-app run dev` on port 3000); (2) the icon catalog at `/stage-plot-icon-preview` is dev-allowlisted in `supabase-middleware.ts` so it renders without a session. Other authed surfaces still need a logged-in session in the preview browser. Icon system foundation = `f881781`; drum core kit + catalog = `494e267`; dev allowlist = `0be6ea6`.

**§SP1 complete (`5c6f9a2`):** 164 built-in icons across 12 categories live in `src/lib/stage-plot/icons/` (registry = `index.ts` → `ALL_ICONS` / `getIcon` / `listIconsByCategory` / `searchIcons`; render via `src/components/stage-plot/StagePlotIcon.tsx`). Authored mostly via a reusable workflow pipeline (author agent per category → JS contract-lint → repair pass → a scratch `scripts/_gen_icons.mjs` generator → wire into index → screenshot-verify). 16 hand-authored (12 core drums + 4 RH/LH composites); rest fan-out. Drums are neutral; each category has a `--lp-plot-cat-*` colour + letter badge. A full instrument-by-instrument review pass then landed (`38c9028` drums+musicians, `4987710` 9 categories) — now **151 icons**: musicians collapsed to person M/F (name+instrument = per-item label), drums refined, realistic strings/mics(SM58,AKG414)/keys/amps(Modeller)/monitors(PSM1000)/signal(stagebox variants)/infra(lightning-bolt power, blank risers)/lighting. Reusable icon pipeline = author-workflow → JS contract-lint → scratch `scripts/_gen_icons.mjs` (generate) / `_merge_icons.mjs` (upsert+delete+riser-blank). NEXT: §SP2 canvas core (halt point — not started). Migration 109 still needs Adam to paste `_apply_109_supabase.sql` into Supabase. Open: Adam floated tinting risers blue (not yet done — would need a per-icon colour override).

Review round 2 (commits 537e034..77e8ff0): strings replaced with Adam's redesigned outline set (+ new strat electric) and an `outline:true` IconDescriptor flag added (line-art icons render unfilled, category-stroke); mics redesigned as traced profile silhouettes (SM58/AKG C414 etc., outline, viewBox 48); drum kits regained cymbals + left-handed variants (6 kits, shells via transforms); female gained hair; power drops use Edison duplex sockets + a separated lightning bolt; amps made depth-accurate (one-cab wide rects); footprint scale fixes (worn mics 0.3ft, amp depths). NOW 154 icons. NOTE: scale audit was targeted (worst offenders), not an exhaustive per-item Google check. Mics + strings render in viewBox 0 0 48 48; everything else 0 0 100 100. Electric guitar is horizontal (footprint 3.4x1.4). Full footprint scale audit applied (commit 7b59c6c). §SP1 DONE.

AUTONOMOUS SESSION (pushed to origin `feat/stage-plot-builder`, build-green): icon review 3 (`4fbe6fe`: drums reoriented throne-bottom/kick-top + cymbals, amps as Marshall cabs, horizontal strat, non-stereotyped female, legible DI, faint+bold grid); functional editor §SP2b/§SP3 (`1864ad3`: palette·canvas·properties, add/select/drag-snap/edit/delete); §SP2c markers + duplicate (`b7dc8a8`); §SP7 PDF (`d7ac81c`: client print + server Puppeteer route); §SP0 DB wiring (`35f4cc9`: server.ts mappers, /api/stage-plots CRUD, /artists/[id]/stage-plots library + editor route — needs migration 109); status doc `c17f884`. TEST NOW (no DB): /stage-plot-editor (localStorage), /stage-plot-icon-preview, /stage-plot-canvas-preview. See docs/handover/CC_STAGE_PLOT_STATUS.md. NOT built: §SP4-6, §SP8-18 + library nav tab + riser-blue.

§SP2 started: §SP2a = `acacec8` — SVG `<StageCanvas>` render surface (src/components/stage-plot/StageCanvas.tsx + src/lib/stage-plot/geometry.ts; 1ft=22px stage-pixel space, fitView, wheel zoom, pan, dotted grid, edge rulers, US/DS/SR/SL cardinals, AUDIENCE, items as nested <svg> reusing icon bodies at footprint scale). Dev harness /stage-plot-canvas-preview (middleware allowlist widened to /stage-plot-*). NEXT: §SP2b selection + drag-drop + snap-to-grid; §SP2c polygon stage extension + reference-marker toggles. Minor: AUDIENCE label sits low (may clip); risers-blue still open.
