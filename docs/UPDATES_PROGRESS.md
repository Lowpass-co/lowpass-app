# Lowpass updates – progress (in order)

## Done

1. **Login** – Forgot password; post-login zoom transition.
2. **Dashboard** – Active Tours → link to tours; Shows Advanced progress bar + data; Shows This Week; Next Show (N days).
3. **Needs Attention** – Grouped by show; HIGH RISK at top; click opens section (hash + expand).
4. **Tour wizard** – Spotify selection animation; date validation + toast; continent dropdown multi-select; principal/band/crew centred.
5. **Routing** – Bus 0.8× / Van 0.9× drive time; travel red &gt;10h or &gt;450mi; transit StyledSelect; tour header compact (Artist | Tour | Dates); Save tour modal icons grey → orange on hover; **day type dropdown** (multi-select, same colours); **calendar feed** counts festivals as show days; **map**: dates on markers, iconic labels (BUS/DRV/VAN), **stable zoom** (FitBoundsOnce); Van option.
6. **Advance** – **Search full address** (venue, city, address); **"Add a section to track progress"** when 0 sections (tour overview); **load fade-in** on section builder; address in advance API + overview type.
7. **Calendar** – **Global calendar**: all show/festival dates from all tours; **toggles** for tours and artists; **month grid** with colour by tour; **click show** → **info card** with links to Advance and Budget (no longer sends you to routing).
8. **Dashboard (completion pass)** – **1a**: "Tours needing attention" / "Shows This Week" replaced with **"Upcoming shows (next 30 days)"** (count of show days in next 30 days, link to `/calendar`). **1b**: **Budget snapshot** section below advance: table per active tour with Tour name (link to budget) | Budget status (% spent) | Net P&L (planned) | Next show date. New API: `GET /api/budget/dashboard-snapshot`.
9. **Advance – Day overview strip (completion §2)** – Left date strip in section builder: **≈64px** wide (`w-16`), pill shows **date** (e.g. "08 Apr") + **city abbreviation**; current show **orange**; **scrollable**; **hidden on mobile** (`hidden md:flex`). Click navigates to that show's advance.
10. **Advance – Days off as pills (completion §3)** – Overview: day-off pills **smaller and subtler** (lp-text-tertiary, no orange border); **"Day Off"** or **"Travel" + city**; click opens **notes modal** (not full advance builder). Modal loads/saves note via `day_off_notes` in advance data; creates minimal advance (POST sections: []) when none exists.
11. **Advance – Item adds into expanded section (§4)** – Verified: adding a field from library (click or drop) sets `lastAddedTemplateId`; useEffect expands that section and collapses others. No code change.
12. **Advance – Contacts always at top (§5)** – `sortFieldsContactsFirst()` helper: contact-type fields (`field.type === 'contact'`) sort to top in SectionCard, RiderCard, and SettlementBlock field lists.
13. **Advance – Drag animation (§6)** – Setup mode: **section** drag gets `scale-[1.02]` + `shadow-lg` + `opacity-90`; **field** row gets `scale-105` + `shadow-md`; **placeholder** at drop target (dashed border, same height) for both section and field reorder; `transition-all duration-200` for smooth shift and snap.
14. **Advance – Custom fields & library (§7)** – **7a**: Optional/Required pill in CustomFieldModal. **7b**: Custom field to section + library; workspace rows show ✕ when not in advance (✓ when in); ✕ → “Delete this advance field?” modal; on confirm PATCH template (remove field) and refetch. **7c**: GET /api/advance/templates workspace-scoped. **7d**: Library Custom section drag-to-reorder; `sort_order` on advance_templates (migration 018); POST /api/advance/templates/reorder. **7e**: “Save as template” works when unsaved: POST /api/advance/layout-templates (no tour_id); workspace table advance_layout_templates (migration 019). **7f**: Delete section from UI + Supabase; plus opens add-field; notes (description) in library.
15. **Advance – Important documents (§8)** – List, fetch from storage, delete, green border for uploaded docs.
16. **Advance – Autosave (§9)** – "Saved!" inline, no layout shift.
17. **Advance – Catering/buyout (§10)** – Add option dropdown (user-wide).
18. **Advance – Meal times (§11)** – + Add meal, type + time, schedulable.
19. **Advance – Rider status (§12)** – Top of Hospitality, same dropdown.
20. **Advance – Flights / Personnel (§13)** – "Personnel on this flight" as pill-style searchable multi-select; "Add flight" / "Add another flight" only when at least one key field (outbound, inbound, airline, ref, allowance) is populated.
21. **Advance – Progress/context when collapsed (§14)** – Progress dropdown and three-dot context menu work when section is collapsed; section auto-expands when either is opened (ContextMenu `onBeforeOpen`, StatusDropdown/AssignDropdown `onClick`).
22. **Advance – Custom contact role (§15)** – Role field: free-text with autocomplete from user-wide lookup; new roles saved via POST /api/advance/options; section label (e.g. "Hospitality Contact") pre-fills role (e.g. "Hospitality") for new contacts; KeyContactsCard and SectionCard pass defaultRole and use RoleCombobox.
23. **Advance – Key contacts search & phone (§16)** – Name search allows spaces (send full nameQuery to API; display nameQuery when typing so space not lost); phone input restricted to digits, spaces, +, (, ), -; RoleCombobox wrapper/panel z-index and isolate so role selector is always visible.
24. **Advance – Flag dropdown visibility (§17)** – SectionCard uses overflow-visible so flag dropdown panel is not clipped; flag trigger and panel z-[200] so dropdown shows when section collapsed or expanded.
25. **Advance – Local info magnifying glass (§18)** – Click search: auto-fill first result; "Auto-filled from Google" badge (clickable) opens popover with same alternatives; field remains editable; selecting from dropdown fills field (selectingRef + handleBlur to avoid onBlur overwrite).
26. **Advance – Dropdown z-index (§19)** – AdvanceDropdownZContext + AdvanceDropdownZProvider; SelectDropdown, AddableSelectDropdown, RoleDropdown, RoleCombobox, StatusDropdown, AssignDropdown use getZIndex() when open so the most recently opened dropdown always renders on top.
27. **Advance – Schedule changeover icon (§20)** – Changeover field in Schedule section uses Lucide Clock icon instead of text label (FieldRenderer labelIcon prop).
28. **Advance – Schedule templates (§21)** – "Save as template" and "Load template" in Schedule section; templates user-wide (all tours) or tour-wide (this tour only); table advance_schedule_templates (migration 021); GET/POST /api/advance/schedule-templates; setSectionData in FillMode to apply loaded template.
29. **Advance – Schedule no asterisks, artist labels (§22)** – Schedule section: hide required asterisk (hideRequiredIndicator); Headliner Soundcheck/Set labels override to "{Artist Name} Soundcheck" and "{Artist Name} Set" when tour has artist (artist_name from advance API); custom schedule item types addable via Edit sections + dropdown field with "+ Add option" (advance_dropdown_options).
30. **Advance – Drive distance editable + auto-populated (§23)** – Auto-fill from routing (previous leg miles/hours); manually editable; "From routing" / "Custom" label; drive_distance_source in data.
31. **Advance – Settlement moved to Budget (§24)** – Settlement removed from Advance UI; remains in Budget (SettlementTab).
32. **Advance – Venue Production Contact optional (§25)** – Required validation removed; hideRequiredIndicator in Production section for production_contact; migration 022.
33. **Advance – Deal Info section (§26)** – New section template (Deal Info) in library with tm_only flag (lock icon); fields: Guarantee, Guest list, Transport from promoter, Backline, Notes, deal memo file; migration 023; advance_templates.tm_only; sections enriched with tm_only in GET; "Upload deal memo" button: document type dropdown, multiple files (same type), POST /api/advance/extract-deal-memo (Anthropic Claude + pdf-parse for PDF); review extracted data step before applying to form.
34. **Profile editor (§27)** – Sidebar name/avatar click opens panel with Log out | Edit profile. Profile page at /profile: Display name, Profile picture (upload via POST /api/profile/avatar → avatars bucket), Email (read-only), Phone, Passport number (optional; stored in profiles.passport_encrypted; mask in GET, clear/change in form), Day rate, Per diem rate. Migration 024 (profiles: phone, passport_encrypted, day_rate, per_diem_rate). Name displayed in title case (toTitleCase) to fix lowercase bug. Personnel page: "Edit your profile" link to /profile for personnel on tours.
35. **Calendar view fixes (§28)** – All shows across tours (API already filter show/festival); tour and artist toggles; click show opens info card with date, venue, city, tour name, links to Advance and Budget (no redirect to routing); tours.artist_name fixed in GET /api/calendar/events.
36. **Sidebar profile area (§29)** – Click name at bottom opens slide-up panel: avatar (or initials), full name, Log out, Edit profile link. Profile fetched from /api/profile for name and avatar_url.

## Not done (remaining from your list)

- **Advance**: Days off as small pills; summary cards; add item into expanded section (collapse others); contacts at top; drag animation; custom fields/library; deal info; docs upload; profile editor; flights/catering/contacts; schedule templates; optional/required pill; custom section delete/plus/notes; template save independently; important docs display; etc.
- **Advance left date strip** (routing/dates on left, day being edited highlighted).
- **Budget**: Further functional fixes and UI overhaul beyond TransportationTab refactor.
