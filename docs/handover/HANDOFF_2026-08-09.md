# LOWPASS — session handoff (2026-08-09, from Fable session 014d3AoW…)

You are continuing a long-running build on **Lowpass**, Adam Rowley's Next.js 16 + Supabase touring-management app. Read this whole file before acting. Adam is tech-savvy, not code-savvy; he wants clarifying questions at real forks (use AskUserQuestion), terse precise answers, no flattery, and he supplies rulings you must not silently reverse.

## Repo + infra facts
- Repo: github.com/Lowpass-co/lowpass-app (public-readable; you CANNOT push — no credentials). Deployed at lowpass-tm-software.vercel.app (push → auto-deploy). Supabase project pcurvjrnfkuagiuocruk.
- Adam's machine: repo at `/Users/lowpass/Documents/lowpass-app`, mounted in the device VM at `~/mnt/lowpass-app`. device_bash: 45s cap, NO network, CANNOT rm/unlink (use `mv` to `_to_delete/`), git ops leave undeletable `.git/index.lock` — so ADAM runs all git in his own terminal.
- **The delivery ritual (proven ~10×):** clone repo fresh in the sandbox (`git clone --depth 1 https://github.com/Lowpass-co/lowpass-app.git /tmp/repo`), `npm install` (works; pip needs --break-system-packages), edit there, commit with author `Adam Rowley <adamgrowley@gmail.com>` + trailers (Co-Authored-By: your model + Claude-Session link), `git format-patch origin/main..HEAD --stdout > x.patch`, VERIFY it applies on a fresh origin clone (`git am`), then `device_commit_files` the patch into the repo folder (SendUserFile first to get a fileUuid; md5 both ends), and give Adam this exact block:
  `cd /Users/lowpass/Documents/lowpass-app && rm -f .git/HEAD.lock .git/index.lock && git am <file>.patch && rm <file>.patch && git push origin main`
- TRAPS learned: (1) his am'd commits get DIFFERENT hashes than yours — always `git ls-remote`/check his HEAD via device_bash before cutting a patch, and rebase your unique commits onto origin/main if needed; (2) never write a file to his disk that a pending patch also touches (git am fails on "already applied" content) — restore the pre-image via `git show origin/main:path` if you do; (3) the device bridge drops constantly — fall back to SendUserFile + download instructions.
- **Gates before every commit (all run in the sandbox):** `NODE_OPTIONS=--max-old-space-size=3584 npx tsc --noEmit` (0), `npx eslint <touched>` (0), `npx vitest run` (30 files / 538 tests; one known flake in RoutingEditor + pdfProbe suites — rerun once), plus the money harnesses via `node --experimental-strip-types`: `src/lib/payroll/reconcile.harness.ts` (72 checks), `src/lib/payroll/fees.test.ts` (27), `src/lib/settlement/reconcile.harness.ts` (40). Money changes MUST keep legacy numbers bit-identical and extend the harness with pins.
- Migrations: hand-pasted by Adam into Supabase SQL editor; MUST be idempotent (ON CONFLICT / IF NOT EXISTS / guarded UPDATE), numbered sequentially (**current max 263**), down-migration comment block. Advance-template constraint from 076: contact-type fields only in 'Key Contacts'.

## State as of handoff
- **Pushed to origin (HEAD = 5b1693b):** payroll canonical flat-seven rates model (migration 261; effective-day counting everywhere; optimistic SpreadsheetGrid commits), lean income sheet (Contracted|Settled|Variance + IncomeDealSlideOver), settlement walk redesign (KPI strip/ledger cards), smoke docs, rider rename + builder fixes, four-front batch (channel list created on its own page tour-wide; rider-centric template catalog = migration 263; native-control kill via globals.css + .lp-select; full settlement waterfall = migration 262 + computeBoxOffice + catch-up batch), day view Tour Book rebuild (flights date-match fix, files card, hero, day-sheet PDF button), rooming Unassigned-Hotel flood fix (placeholder naming/reuse/heal + budget label `{hotel} — {city} · {date} · N rooms` + Nights-tab router.refresh).
- **On Adam's disk, NOT yet applied:** `round2-tail.patch` (3 commits: smoke rebuild → spine/keyboard fixes → SP/KEY smokes). He has the command block. Stale `rider-settlement-batch.patch` sits beside it (his block deletes both).
- **Supabase pastes outstanding:** 262 then 263 (261 first if never pasted; also possibly 241/257/259/260 from earlier — ask). All re-pastable. **Never re-paste 258 after 263** (reverts the rider catalog).
- Smoke page `docs/smoke-tests/SMOKE_SESSION_2026-08-05.html`: 85 items / 18 sections. Adam's last walk: 25 pass, PF-3 fail. **PF-3 resolved analytically:** hello@ is admin/owner of its own ACTIVE workspace; readonly membership is in workspace 9d3cd337. Retest = switch workspace first. He still owes the `select name, workspace_id from tours order by created_at desc limit 5` check (tenancy confirmation) — if test tours landed in 9d3cd337 it's a real P0.

## Open backlog (Adam-acknowledged, unscheduled)
1. Guest list as structured per-show data (ATOM parity; needs migration).
2. Settlement phase 4: the pretty printable settlement PDF (upgrade export/pdf route to the waterfall layout + XLSX round-trip); spec §3 in `docs/design/SETTLEMENT_ACTUALS_SPEC_2026-08-06.md`. Open questions Q4 (payee/bank fields) + Q5 (day_of vs reconciled collapse).
3. Income phase 5: retire Stage-5 conflict machinery (`overwrite_manual_actuals`) + non-show income manual lines.
4. Invite-flow ruling: accepting an invite should probably make the inviting workspace active (PF-3 fallout — every readonly invitee lands in their own empty admin sandbox).
5. Orphans flagged not deleted: RateTypeToolbar, PayrollSummary, /api/budget/rate-types, RiderSectionLibrary, TourFilesClient. NavRail pre-existing ref-during-render lint.
6. Daysheet-page visual pass done; Adam may report layout nits from the walk — the rider builder's stacked-cards screenshot may have been the SHOW-mode canvas, unverified (ask for the URL if he says stacking persists).
7. Persisted weekly payroll totals (tour-overview cost-to-date) refresh only on repaint/Generate.

## Doctrine (do not violate)
- One counting path for money: `fees.countDayStatuses` + `effectiveStatuses` (payroll) / `computeWalk`+`computeBoxOffice` via `resolvePersonLines` analog (settlement). Every reader through the SSOT; never a second formula.
- Day statuses: show/travel(+legacy off_travel)/rehearsal/promo_radio/off/pd_only/no_tour. OFF bills travel rate + PD; pd_only = PD only; no_tour = nothing. Press/Radio bills its own rate only when set, else show (Dillon ruling). Rates stack (sum) with a ⚠ on flat-day+specific conflicts.
- Settled income figures are walk-authored only; Manual override is the one explicit escape hatch (chips AUTO/MANUAL).
- Money-moving suggestions (e.g. waterfall→overage) require an explicit user click, never auto-write.
- Design grammar ("the ATOM bar", Adam-approved): hue bound to meaning (orange=contracted/outstanding, green=settled, amber=manual/due), heroes with eyebrow/headline/chip, KPI strips, mono tabular ledgers with ruled totals, empty states that invite. Mirror SettlementWalkClient / IncomeDealSlideOver.
- Keyboard contract (TB grammar) everywhere: Tab never walks a list, Enter commits + advances, arrows move highlights, Enter never dead — via CellNav/onEnterCommit, no new focus systems.
- Metadata JSONB for zero-migration features; flag orphans, don't delete; react-hooks/set-state-in-effect → queueMicrotask; PostgREST alias syntax `alias:column`; delegate big builds to general-purpose agents with staged inputs + exact contracts + gates, integrate and re-gate yourself.

First move: device_bash `cd ~/mnt/lowpass-app && git log --oneline -3 && git status --short` to learn what Adam actually applied/pushed since this file was written, then ask him what's next.
