/**
 * UX08 prompt name: one SlideOver is orchestrated by the provider. Prefer `EntityRoutingProvider` import.
 */
export { EntityRoutingProvider as EntitySlideOverHost } from './EntityRoutingContext';
export { useEntityRouting, useEntityRoutingIfPresent } from './EntityRoutingContext';
