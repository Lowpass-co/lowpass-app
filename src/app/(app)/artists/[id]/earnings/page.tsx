/* ============================================
   LOWPASS — /artists/[id]/earnings (Sprint 10 §1.4 — placeholder)
   ============================================ */

import { ArtistScopePlaceholder } from '@/components/artists/ArtistScopePlaceholder';

export default function ArtistEarningsPage() {
  return (
    <ArtistScopePlaceholder
      title="Earnings"
      description="Artist earnings: per-tour revenue + commission breakdowns + cumulative artist totals — coming Sprint 11. Per-tour budget summaries are available under each tour's Budget product today."
    />
  );
}
