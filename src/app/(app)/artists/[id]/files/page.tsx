/* ============================================
   LOWPASS — /artists/[id]/files (Sprint 10 §1.4 — placeholder)
   ============================================ */

import { ArtistScopePlaceholder } from '@/components/artists/ArtistScopePlaceholder';

export default function ArtistFilesPage() {
  return (
    <ArtistScopePlaceholder
      title="Files"
      description="Artist-level file library: brand assets, riders, rate cards, contract templates — coming Sprint 11. Per-tour Files lives under each tour's Operations product today."
    />
  );
}
