/* ============================================
   LOWPASS — /artists/[id]/contracts (Sprint 10 §1.4 — placeholder)
   ============================================ */

import { ArtistScopePlaceholder } from '@/components/artists/ArtistScopePlaceholder';

export default function ArtistContractsPage() {
  return (
    <ArtistScopePlaceholder
      title="Contracts"
      description="Artist contracts: management agreements, label deals, and per-territory licensing — coming Sprint 11."
    />
  );
}
